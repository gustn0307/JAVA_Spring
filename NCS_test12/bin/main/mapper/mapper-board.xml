<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ncs12.mapper.BoardMapper">
  
  <select id="findAll" resultType="ncs12.domain.dto.BoardDto">
  	select * from board 
  	order by no desc 
  </select>
</mapper>